#!/bin/bash

sleep 5

echo "Creating sensor's profile"
prelude-admin register "prelude-lml" "idmef:w" manager --uid 0 --gid 0 --passwd-file=/prelude-registration || :

echo "Starting LML"
exec /usr/sbin/prelude-lml
